{% extends 'front.base.html.twig' %}
    {% block body %}

        <section class="hero hero-normal" >
            <div style="width:900px; margin:0 auto;">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-9">
                            <div class="hero__search">
                                <div class="hero__search__form">
                                    <form method="post" action="{{ path('rechercheP') }}">
                                        <input type=type="text" name="recherche"> <input type="submit" value="rechercher">
                                        <button type="submit" class="site-btn">SEARCH</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>



        <section class="blog-details-hero set-bg" data-setbg="img/5.jpg" style="background-image: url(&quot;img/blog/details/details-hero.jpg&quot;);">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="blog__details__hero__text">
                            <h2 style="color:#151515 " ;>ASTUCES</h2>
                        </div>
                    </div>
                </div>
            </div>
        </section>



        {% for astuce in astuces  %}
            <div style="width:800px; margin:0 auto;">
                <div class="blog-entry align-self-stretch">
                    <a href="blog-single.html" class="block-20 rounded img-2" style="background-image:{{ asset('uploads/'~astuce.image) }};">
                    </a>
                    <div class="text mt-3">
                        <div class="meta mb-2">
                            <div class="blog__details__text">
                                <h3 align="center">{{ astuce.titre }}</h3>
                                <img align="center" src="{{ asset('uploads/'~astuce.image) }}" width="1000" height="500" alt="">
                                <a class="btn btn-success" href="{{ path ('details',{'id': astuce.id}) }}">je découvre</a>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}

        <div class="product__pagination blog__pagination">
            <div style="width:900px; margin:0 auto;">
            {{ knp_pagination_render(astuces) }}
        </div>
        </div>






    {% endblock %}

