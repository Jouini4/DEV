
{% extends 'front.base.html.twig' %}

    {% block body %}






        <section class="breadcrumb-section set-bg" data-setbg="img/1.jpg">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12 text-center">
                        <div class="breadcrumb__text">
                            <h2 style="color:#9cfda4" ;>VIDEOS DIY</h2>
                        </div>
                    </div>
                </div>
            </div>

        </section>

        <div style="width:800px; margin:0 auto;">


            <tbody>
            {% for video in videos %}

                <div class="blog-entry align-self-stretch">

                    </a>
                    <div class="text mt-3">
                        <div class="meta mb-2">

                            <div class="blog__details__text">
                                <h3 align="center">{{ video.titre }}</h3>

                                <video controls >
                                    <source align="center" src="{{ asset('uploads/'~video.titre) }}" width="500" height="100" alt="" type="video/mp4" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen>
                                </video>
                                <p>{{ video.description }}</p>

                            </div>
                        </div>
                    </div>
                </div>






            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
            </table>
        </div>


    {% endblock %}







