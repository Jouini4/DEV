{% extends 'back.base.html.twig' %}
{% block body %}
<div style="width:800px; margin:0 auto;">
    <title>Astuce</title>
    <h1>Astuce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ astuce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ astuce.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ astuce.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img style="margin-bottom: -18px" src="{{ asset('uploads/' ~ astuce.image) }}" width="100" height="100"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backastuce') }}">back to list</a>

    <a href="{{ path('astuce_edit', {'id': astuce.id}) }}">edit</a>

    {{ include('astuce/_delete_form.html.twig') }}
</div>


{% endblock %}
